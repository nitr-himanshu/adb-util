# ADB-UTIL Requirements
# 
# This file is kept for backward compatibility.
# For new installations, use the requirements in the requirements/ directory:
#
# Development setup:
#   pip install -r requirements/dev.txt
#
# Production setup:
#   pip install -r requirements/prod.txt
#
# Or use the modern installation method:
#   pip install -e .

# Default to development requirements for backward compatibility
-r requirements/dev.txt
